/**
 * Callout block extension for Tiptap.
 * Requires @tiptap/core and @tiptap/react to be installed.
 *
 * Supports 4 callout types: info, warning, danger, success.
 * Each renders with a colored left border, icon, and editable content.
 */
"use client";

// TODO: Uncomment when @tiptap packages are installed
// import { Node, mergeAttributes } from "@tiptap/core";
// import { ReactNodeViewRenderer, NodeViewWrapper, NodeViewContent } from "@tiptap/react";

// Placeholder export — the real Callout node extension is commented above.
// Once Tiptap is installed, this will export a proper Node.create() extension.
export const Callout = null;
